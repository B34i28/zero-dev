import { UserOperationStruct } from '@zerodevapp/contracts';
import { PaymasterAPI } from './PaymasterAPI';
import { Provider } from '@ethersproject/abstract-provider';
import { MultiSendCall } from '../multisend';
export declare class TokenPaymasterAPI extends PaymasterAPI {
    readonly projectId: string;
    readonly paymasterUrl: string;
    readonly chainId: number;
    readonly entryPointAddress: string;
    readonly gasTokenAddress: string;
    readonly paymasterAddress: string;
    constructor(projectId: string, paymasterUrl: string, chainId: number, entryPointAddress: string, gasTokenAddress: string, paymasterAddress: string);
    createGasTokenApprovalRequest(provider: Provider): Promise<MultiSendCall>;
    getPaymasterResp(userOp: Partial<UserOperationStruct>, erc20UserOp: Partial<UserOperationStruct>): Promise<object | undefined>;
}
