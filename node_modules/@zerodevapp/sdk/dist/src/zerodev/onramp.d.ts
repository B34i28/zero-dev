import { Signer } from "@ethersproject/abstract-signer";
export type OnrampUserInfo = TransakOnrampUserInfo | SardineOnrampUserInfo;
export interface OnrampOptions {
    signer: Signer;
    userInfo?: OnrampUserInfo;
    defaultToken?: string;
    staging?: boolean;
}
export interface TransakOnrampUserInfo {
    firstName: string;
    lastName: string;
    email: string;
    mobileNumber: string;
    dob: string;
    address: {
        addressLine1: string;
        addressLine2: string;
        city: string;
        state: string;
        postCode: string;
        countryCode: string;
    };
}
export interface SardineOnrampUserInfo {
}
export declare function onramp(options: OnrampOptions): Promise<void>;
